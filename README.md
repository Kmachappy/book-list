To reset your code to look like mine:

```bash
git fetch --all
git reset --hard origin/main
```

If you want to update your remote links
```bash
git remote set-url origin git@git.generalassemb.ly:shawkitajima/bookList.git
```